import { queryByDisplayValue } from '@testing-library/react';
import React from 'react';
const AddNewMovie = ({handleClose, show}) => {
    return ( 
    <div>

    </div>
    );
}
 
export default AddNewMovie;